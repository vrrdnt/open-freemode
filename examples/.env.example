# Copy outside Git and supply with Docker --env-file, or set private Pelican variables.
# Empty secrets intentionally fail validation. See docs/development.md.
SERVER_NAME=Open Freemode
MAX_PLAYERS=30
# Standalone port. Pelican's built-in SERVER_PORT takes precedence.
GAME_PORT=30120

FIVEM_LICENSE_KEY=
DB_HOST=database.example.invalid
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=
